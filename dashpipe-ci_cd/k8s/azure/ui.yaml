apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashpipe-ui
  namespace: dashpipe
  labels:
    app: dashpipe-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashpipe-ui
  template:
    metadata:
      labels:
        app: dashpipe-ui
    spec:
      containers:
        - name: ui
          image: dashpipe/ui:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: dashpipe-ui
  namespace: dashpipe
spec:
  selector:
    app: dashpipe-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: LoadBalancer
