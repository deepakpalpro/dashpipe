# `plet-webhook-source`

Category: **Source**. Connector: `event_listener`. Behavior: `source_event`.

Build: `docker build -f pipelets/plet-webhook-source/Dockerfile -t dashpipe/plet-webhook-source:local pipelets`
