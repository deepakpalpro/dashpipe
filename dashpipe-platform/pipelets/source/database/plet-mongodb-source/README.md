# `plet-mongodb-source`

Category: **Source**. Connector: `db`. Behavior: `source_jdbc`.

Build: `docker build -f pipelets/plet-mongodb-source/Dockerfile -t dashpipe/plet-mongodb-source:local pipelets`
