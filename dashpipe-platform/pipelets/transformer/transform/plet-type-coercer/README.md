# `plet-type-coercer`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-type-coercer/Dockerfile -t dashpipe/plet-type-coercer:local pipelets`
