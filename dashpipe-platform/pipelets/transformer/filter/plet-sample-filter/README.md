# `plet-sample-filter`

Category: **Processor**. Connector: `none`. Behavior: `proc_sample`.

Build: `docker build -f pipelets/plet-sample-filter/Dockerfile -t dashpipe/plet-sample-filter:local pipelets`
