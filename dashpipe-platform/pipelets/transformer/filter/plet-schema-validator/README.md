# `plet-schema-validator`

Category: **Processor**. Connector: `none`. Behavior: `proc_schema`.

Build: `docker build -f pipelets/plet-schema-validator/Dockerfile -t dashpipe/plet-schema-validator:local pipelets`
