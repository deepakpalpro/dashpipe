# `plet-currency-fx`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-currency-fx/Dockerfile -t dashpipe/plet-currency-fx:local pipelets`
