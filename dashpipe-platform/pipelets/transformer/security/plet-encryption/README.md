# `plet-encryption`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-encryption/Dockerfile -t dashpipe/plet-encryption:local pipelets`
