# `plet-language-detect`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-language-detect/Dockerfile -t dashpipe/plet-language-detect:local pipelets`
