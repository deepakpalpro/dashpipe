# `plet-regex-extract`

Category: **Processor**. Connector: `none`. Behavior: `proc_regex`.

Build: `docker build -f pipelets/plet-regex-extract/Dockerfile -t dashpipe/plet-regex-extract:local pipelets`
