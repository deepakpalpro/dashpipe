# `plet-gcs-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-gcs-destination/Dockerfile -t dashpipe/plet-gcs-destination:local pipelets`
