# `plet-archive-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-archive-destination/Dockerfile -t dashpipe/plet-archive-destination:local pipelets`
