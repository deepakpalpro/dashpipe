# `plet-ftp-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-ftp-destination/Dockerfile -t dashpipe/plet-ftp-destination:local pipelets`
