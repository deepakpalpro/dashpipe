# `plet-adls-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-adls-destination/Dockerfile -t dashpipe/plet-adls-destination:local pipelets`
