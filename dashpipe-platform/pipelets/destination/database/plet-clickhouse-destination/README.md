# `plet-clickhouse-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-clickhouse-destination/Dockerfile -t dashpipe/plet-clickhouse-destination:local pipelets`
