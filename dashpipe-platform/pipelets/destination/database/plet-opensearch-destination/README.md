# `plet-opensearch-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-opensearch-destination/Dockerfile -t dashpipe/plet-opensearch-destination:local pipelets`
