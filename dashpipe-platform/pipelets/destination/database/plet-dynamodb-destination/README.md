# `plet-dynamodb-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-dynamodb-destination/Dockerfile -t dashpipe/plet-dynamodb-destination:local pipelets`
