# `plet-redis-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-redis-destination/Dockerfile -t dashpipe/plet-redis-destination:local pipelets`
