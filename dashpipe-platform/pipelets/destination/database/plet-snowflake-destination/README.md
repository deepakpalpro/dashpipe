# `plet-snowflake-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-snowflake-destination/Dockerfile -t dashpipe/plet-snowflake-destination:local pipelets`
