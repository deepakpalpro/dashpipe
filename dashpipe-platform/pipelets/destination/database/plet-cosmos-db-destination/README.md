# `plet-cosmos-db-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-cosmos-db-destination/Dockerfile -t dashpipe/plet-cosmos-db-destination:local pipelets`
