# `plet-jdbc-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-jdbc-destination/Dockerfile -t dashpipe/plet-jdbc-destination:local pipelets`
