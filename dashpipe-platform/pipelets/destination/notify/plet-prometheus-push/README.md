# `plet-prometheus-push`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-prometheus-push/Dockerfile -t dashpipe/plet-prometheus-push:local pipelets`
