# `plet-datadog-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-datadog-destination/Dockerfile -t dashpipe/plet-datadog-destination:local pipelets`
