# `plet-teams-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-teams-destination/Dockerfile -t dashpipe/plet-teams-destination:local pipelets`
