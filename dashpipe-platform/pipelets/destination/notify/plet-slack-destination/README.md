# `plet-slack-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-slack-destination/Dockerfile -t dashpipe/plet-slack-destination:local pipelets`
