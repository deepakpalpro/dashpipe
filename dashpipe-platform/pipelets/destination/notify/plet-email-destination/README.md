# `plet-email-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-email-destination/Dockerfile -t dashpipe/plet-email-destination:local pipelets`
