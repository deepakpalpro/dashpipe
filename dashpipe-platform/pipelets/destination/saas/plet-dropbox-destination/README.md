# `plet-dropbox-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-dropbox-destination/Dockerfile -t dashpipe/plet-dropbox-destination:local pipelets`
