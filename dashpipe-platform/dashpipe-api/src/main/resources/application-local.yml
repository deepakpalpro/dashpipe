# Local / Compose: apply schema + demo seed (tenants, connectors).
# Azure empty stack uses profile k8s only → schema migrations without seed.
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/seed
