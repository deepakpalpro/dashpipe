# `plet-nats-source`

Category: **Source**. Connector: `message_bus`. Behavior: `source_bus`.

Build: `docker build -f pipelets/plet-nats-source/Dockerfile -t dashflow/plet-nats-source:local pipelets`
