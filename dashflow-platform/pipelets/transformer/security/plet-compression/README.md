# `plet-compression`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-compression/Dockerfile -t dashflow/plet-compression:local pipelets`
