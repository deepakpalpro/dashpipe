# `plet-schema-validator`

Category: **Processor**. Connector: `none`. Behavior: `proc_schema`.

Build: `docker build -f pipelets/plet-schema-validator/Dockerfile -t dashflow/plet-schema-validator:local pipelets`
