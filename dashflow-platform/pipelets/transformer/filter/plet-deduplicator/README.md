# `plet-deduplicator`

Category: **Processor**. Connector: `none`. Behavior: `proc_dedupe`.

Build: `docker build -f pipelets/plet-deduplicator/Dockerfile -t dashflow/plet-deduplicator:local pipelets`
