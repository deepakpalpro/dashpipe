# `plet-entity-extract`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-entity-extract/Dockerfile -t dashflow/plet-entity-extract:local pipelets`
