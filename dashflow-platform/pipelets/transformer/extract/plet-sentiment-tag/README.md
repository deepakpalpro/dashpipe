# `plet-sentiment-tag`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-sentiment-tag/Dockerfile -t dashflow/plet-sentiment-tag:local pipelets`
