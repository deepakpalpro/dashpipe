# `plet-regex-extract`

Category: **Processor**. Connector: `none`. Behavior: `proc_regex`.

Build: `docker build -f pipelets/plet-regex-extract/Dockerfile -t dashflow/plet-regex-extract:local pipelets`
