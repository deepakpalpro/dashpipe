# `plet-lookup-cache`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-lookup-cache/Dockerfile -t dashflow/plet-lookup-cache:local pipelets`
