# `plet-branch-router`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-branch-router/Dockerfile -t dashflow/plet-branch-router:local pipelets`
