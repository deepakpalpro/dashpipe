# `plet-rate-limiter`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-rate-limiter/Dockerfile -t dashflow/plet-rate-limiter:local pipelets`
