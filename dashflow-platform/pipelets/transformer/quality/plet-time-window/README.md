# `plet-time-window`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-time-window/Dockerfile -t dashflow/plet-time-window:local pipelets`
