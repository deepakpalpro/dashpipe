# `plet-retry-buffer`

Category: **Processor**. Connector: `none`. Behavior: `proc_passthrough`.

Build: `docker build -f pipelets/plet-retry-buffer/Dockerfile -t dashflow/plet-retry-buffer:local pipelets`
