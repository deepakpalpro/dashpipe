# `plet-kafka-destination`

Category: **Destination**. Connector: `message_bus`. Behavior: `dest_bus`.

Build: `docker build -f pipelets/plet-kafka-destination/Dockerfile -t dashflow/plet-kafka-destination:local pipelets`
