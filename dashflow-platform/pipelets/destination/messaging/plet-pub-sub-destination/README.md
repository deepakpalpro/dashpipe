# `plet-pub-sub-destination`

Category: **Destination**. Connector: `message_bus`. Behavior: `dest_bus`.

Build: `docker build -f pipelets/plet-pub-sub-destination/Dockerfile -t dashflow/plet-pub-sub-destination:local pipelets`
