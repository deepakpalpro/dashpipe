# `plet-sqs-destination`

Category: **Destination**. Connector: `message_bus`. Behavior: `dest_bus`.

Build: `docker build -f pipelets/plet-sqs-destination/Dockerfile -t dashflow/plet-sqs-destination:local pipelets`
