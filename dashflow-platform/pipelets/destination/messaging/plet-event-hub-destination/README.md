# `plet-event-hub-destination`

Category: **Destination**. Connector: `message_bus`. Behavior: `dest_bus`.

Build: `docker build -f pipelets/plet-event-hub-destination/Dockerfile -t dashflow/plet-event-hub-destination:local pipelets`
