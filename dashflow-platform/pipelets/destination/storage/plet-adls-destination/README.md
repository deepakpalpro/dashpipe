# `plet-adls-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-adls-destination/Dockerfile -t dashflow/plet-adls-destination:local pipelets`
