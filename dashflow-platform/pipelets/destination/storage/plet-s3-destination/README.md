# `plet-s3-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-s3-destination/Dockerfile -t dashflow/plet-s3-destination:local pipelets`
