# `plet-pagerduty-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-pagerduty-destination/Dockerfile -t dashflow/plet-pagerduty-destination:local pipelets`
