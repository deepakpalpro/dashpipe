# `plet-prometheus-push`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-prometheus-push/Dockerfile -t dashflow/plet-prometheus-push:local pipelets`
