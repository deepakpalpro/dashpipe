# `plet-redshift-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-redshift-destination/Dockerfile -t dashflow/plet-redshift-destination:local pipelets`
