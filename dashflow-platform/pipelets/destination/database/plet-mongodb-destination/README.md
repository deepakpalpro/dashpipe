# `plet-mongodb-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-mongodb-destination/Dockerfile -t dashflow/plet-mongodb-destination:local pipelets`
