# `plet-bigquery-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-bigquery-destination/Dockerfile -t dashflow/plet-bigquery-destination:local pipelets`
