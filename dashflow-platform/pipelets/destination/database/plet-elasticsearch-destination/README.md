# `plet-elasticsearch-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-elasticsearch-destination/Dockerfile -t dashflow/plet-elasticsearch-destination:local pipelets`
