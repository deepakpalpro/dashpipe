# `plet-databricks-destination`

Category: **Destination**. Connector: `db`. Behavior: `dest_jdbc`.

Build: `docker build -f pipelets/plet-databricks-destination/Dockerfile -t dashflow/plet-databricks-destination:local pipelets`
