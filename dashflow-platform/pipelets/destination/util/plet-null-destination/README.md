# `plet-null-destination`

Category: **Destination**. Connector: `none`. Behavior: `dest_null`.

Build: `docker build -f pipelets/plet-null-destination/Dockerfile -t dashflow/plet-null-destination:local pipelets`
