# `plet-salesforce-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-salesforce-destination/Dockerfile -t dashflow/plet-salesforce-destination:local pipelets`
