# `plet-sharepoint-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-sharepoint-destination/Dockerfile -t dashflow/plet-sharepoint-destination:local pipelets`
