# `plet-hubspot-destination`

Category: **Destination**. Connector: `rest`. Behavior: `dest_rest`.

Build: `docker build -f pipelets/plet-hubspot-destination/Dockerfile -t dashflow/plet-hubspot-destination:local pipelets`
