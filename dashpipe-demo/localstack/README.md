# LocalStack (S3/SQS) for dashpipe-demo

Uses the upstream `localstack/localstack` image via root `docker-compose.yml` / `dashpipe-demo/docker-compose.demo.yml`.

Smoke test:

```bash
./dashpipe-demo/scripts/smoke-localstack.sh
```
