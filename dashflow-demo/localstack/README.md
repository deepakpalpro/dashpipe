# LocalStack (S3/SQS) for dashflow-demo

Uses the upstream `localstack/localstack` image via root `docker-compose.yml` / `dashflow-demo/docker-compose.demo.yml`.

Smoke test:

```bash
./dashflow-demo/scripts/smoke-localstack.sh
```
